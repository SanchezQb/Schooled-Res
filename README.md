Application with CRUD operations for Students Resource

Run ```yarn install``` to grab the dependencies
Run ```yarn start``` to start the development server
Run ```yarn build``` to create an optimized production build